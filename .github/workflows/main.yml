name: Deploy to SFTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to SFTP
        uses: jakejarvis/sftp-deploy-action@4.2.3
        with:
          server: ${{ secrets.SERVER }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          local_dir: ./  # Chemin vers le répertoire contenant votre page HTML et autres fichiers
          remote_path: /var/www/portfolio/  # Chemin vers le répertoire de destination sur le serveur SFTP
